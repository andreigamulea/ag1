<!DOCTYPE html>
<html lang="ro">
  <head>
    <meta charset="UTF-8">
    <title><%= content_for?(:title) ? yield(:title) : "AYUS GRUP Romania" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <%# Meta robots - default index, follow %>
    <% if content_for?(:meta_robots) %>
      <%= yield(:meta_robots) %>
    <% else %>
      <meta name="robots" content="index, follow">
    <% end %>

    <%# Canonical URL - previne duplicate %>
    <% if content_for?(:canonical) %>
      <link rel="canonical" href="<%= yield(:canonical) %>">
    <% end %>

    <%# Meta description pentru SEO %>
    <% if content_for?(:meta_description) %>
      <meta name="description" content="<%= yield(:meta_description) %>">
    <% else %>
      <meta name="description" content="AYUS GRUP - Produse naturale Ayurveda in Romania. Suplimente, carti si produse pentru sanatate.">
    <% end %>

    <%# Open Graph meta tags %>
    <meta property="og:site_name" content="AYUS GRUP Romania">
    <meta property="og:locale" content="ro_RO">
    <% if content_for?(:og_type) %>
      <meta property="og:type" content="<%= yield(:og_type) %>">
    <% else %>
      <meta property="og:type" content="website">
    <% end %>
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) : 'AYUS GRUP Romania' %>">
    <meta property="og:description" content="<%= content_for?(:meta_description) ? yield(:meta_description) : 'AYUS GRUP - Produse naturale Ayurveda in Romania.' %>">
    <% if content_for?(:og_image) %>
      <meta property="og:image" content="<%= yield(:og_image) %>">
    <% end %>
    <% if content_for?(:canonical) %>
      <meta property="og:url" content="<%= yield(:canonical) %>">
    <% end %>

    <%# JSON-LD Structured Data for SEO %>
    <%= render 'shared/json_ld_schema' %>

    <!-- Doar aici adaugi Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%= javascript_importmap_tags %>
  </head>

  <body class="shop-layout">
    <%= render "layouts/header" %>
    <div class="shop-container">
      <div class="shop-content">
        <%= yield %>
      </div>
    </div>
    <%= render 'layouts/newsleter' %>
    <%= render 'layouts/footer' %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
