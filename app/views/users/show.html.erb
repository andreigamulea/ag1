<div class="container_content user-details">
  <h1 class="admin-title">👤 Detalii utilizator</h1>

  <div class="details-group">
    <p><strong>ID:</strong> <%= @user.id %></p>
    <p><strong>Email:</strong> <%= @user.email %></p>
    <p><strong>Rol:</strong>
      <%= case @user.role
          when 0 then "Client"
          when 1 then "Administrator"
          when 2 then "Manager"
          else "Necunoscut"
          end %>
    </p>
    <p><strong>Creat la:</strong> <%= @user.created_at.strftime("%Y-%m-%d %H:%M") %></p>
    <p><strong>Ultima actualizare:</strong> <%= @user.updated_at.strftime("%Y-%m-%d %H:%M") %></p>
  </div>

  <div class="details-actions">
    <%= link_to "← Înapoi", users_path, class: "form-button secondary" %>
    <%= link_to "✏️ Editează", edit_user_path(@user), class: "form-button primary" %>
  </div>
</div>