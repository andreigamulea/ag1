<div class="container_content user-list">
  <h1 class="admin-title">👥 Listă utilizatori</h1>

  <%= link_to "➕ Adaugă utilizator", new_user_path, class: "form-button", style: "margin-bottom: 1.5rem;" %>

  <div class="table-wrapper">
    <table class="styled-table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Email</th>
          <th>Rol</th>
          <th>Status</th>
          <th>Creat la</th>
          <th>Acțiuni</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.email %></td>
            <td>
              <%= case user.role
                  when 0 then "Client"
                  when 1 then "Administrator"
                  when 2 then "Manager"
                  else "Necunoscut"
                  end %>
            </td>
            <td>
              <% if user.active? %>
                <span class="status-badge active">✓ Activ</span>
              <% else %>
                <span class="status-badge inactive">✗ Inactiv</span>
              <% end %>
            </td>
            <td><%= user.created_at.strftime("%Y-%m-%d %H:%M") %></td>
            <td class="table-actions">
              <%= link_to "Detalii", user_path(user), class: "table-button neutral" %>
              <%= link_to "Editează", admin_edit_user_path(user), class: "table-button primary" %>
              
              <% unless user.active? %>
                <%= button_to "🔓 Reactivează", 
                    reactivate_user_path(user), 
                    method: :patch,
                    class: "table-button success",
                    form: { style: "display: inline;" } %>
              <% end %>
              
              <% if user != current_user %>
                <%= link_to "Șterge", user_path(user), method: :delete, data: { confirm: "Ești sigur?" }, class: "table-button danger" %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

