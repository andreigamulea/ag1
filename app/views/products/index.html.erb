<div class="container_content product-list">
  <h1 class="admin-title">🛒 Listă produse</h1>

  <div class="product-actions">
    <%= link_to "➕ Adaugă produs", new_product_path, class: "form-button" %>
  </div>

  <div class="table-wrapper">
    <table class="styled-table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Nume</th>
          <th>Preț</th>
          <th>Stoc</th>
          <th>Status</th>
          <th>Acțiuni</th>
        </tr>
      </thead>
      <tbody>
        <% @products.each do |product| %>
          <tr>
            <td><%= product.id %></td>
            <td><%= product.name %></td>
            <td><%= number_to_currency(product.price, unit: "RON ") %></td>
            <td><%= product.stock %></td>
            <td><%= product.status.capitalize %></td>
            <td class="table-actions">
              <%= link_to "Detalii", product_path(product), class: "table-button neutral" %>
              <%= link_to "Editează", edit_product_path(product), class: "table-button primary" %>
              <%= link_to "Șterge", product_path(product), method: :delete, data: { confirm: "Ești sigur că vrei să ștergi acest produs?" }, class: "table-button danger" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<style>
.admin-title {
  font-size: 28px;
  color: #8b0000;
  text-align: center;
  margin-bottom: 20px;
  padding-top: 20px;
}

.product-actions {
  text-align: center;
  margin-bottom: 20px;
}

.form-button {
  background-color: #8b0000;
  color: white;
  border: none;
  padding: 10px 22px;
  font-size: 1rem;
  font-weight: bold;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  transition: background-color 0.3s ease;
}

.form-button:hover {
  background-color: #fbc02d;
  color: #8b0000;
}

.table-wrapper {
  overflow-x: auto;
}

.styled-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 15px;
  text-align: left;
}

.styled-table thead {
  background-color: #8b0000;
  color: white;
}

.styled-table th,
.styled-table td {
  padding: 12px;
  border-bottom: 1px solid #ddd;
  vertical-align: middle;
}

.styled-table tbody tr:hover {
  background: #fef3dc;
}

.table-actions {
  white-space: nowrap;
}

/* Butoane tabel */
.table-button {
  padding: 6px 12px;
  font-size: 0.875rem;
  font-weight: bold;
  text-decoration: none;
  border-radius: 4px;
  margin-right: 5px;
  display: inline-block;
  transition: background-color 0.2s ease-in-out;
}

.table-button.neutral {
  background-color: #f5f5f5;
  color: #8b0000;
  border: 1px solid #8b0000;
}

.table-button.primary {
  background-color: #8b0000;
  color: white;
}

.table-button.danger {
  background-color: #bc2c2c;
  color: white;
}

.table-button:hover {
  opacity: 0.9;
}

</style>
